/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/acm-srm-website', 
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig   